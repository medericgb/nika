<h1>Products#index</h1>
<p>Find me in app/views/products/index.html.erb</p>

<% @products.each do |product| %>
  <p>
    <%= product.code %> / <%= product.name %>, <%= product.price %> <%= link_to "Add to cart", add_to_cart(product) %>
  </p>
<% end %>
